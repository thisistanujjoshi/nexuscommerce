# Staging overrides: production-like, pinned image tag, moderate scale.
image:
  tag: "1.0.0-rc"

ingress:
  host: staging.nexuscommerce.local

services:
  gateway:
    replicas: 2
  catalog:
    replicas: 2
  orders:
    replicas: 2
