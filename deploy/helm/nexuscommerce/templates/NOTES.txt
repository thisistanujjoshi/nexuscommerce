NexusCommerce {{ .Chart.AppVersion }} deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Services deployed:
{{- range $name, $svc := .Values.services }}
  - {{ $name }} ({{ $svc.replicas | default 1 }} replica(s))
{{- end }}

The gateway is the only public entry point.
{{- if .Values.ingress.enabled }}
Reach it at: http://{{ .Values.ingress.host }}/
(add "{{ .Values.ingress.host }}" to your hosts file pointing at the ingress controller's external IP)
{{- else }}
Ingress is disabled; port-forward the gateway to reach it:
  kubectl port-forward svc/gateway 5100:{{ .Values.services.gateway.port }}
{{- end }}

Get a token, then call through the gateway:
  curl -X POST http://<host>/auth/token -H "Content-Type: application/json" \
    -d '{"username":"demo","password":"demo123"}'
